# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  username: storystreams
  encoding: utf8
  host: localhost
  port: 5432
  pool: 10

development:
  <<: *default
  database: storystreams_development
  password:


# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: storystreams_test
  password:

production:
  <<: *default
  database: storystreams
  password: "***REMOVED***"

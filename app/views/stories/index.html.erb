<h1>Story Streams</h1>

<table>

  <tbody>
    <tr>
      <% @stories.each do |story| %>
	<td><%= link_to(image_tag(story.pictures[0].filename_url(:thumb)), story) if story.pictures? %></td>
      <% end %>
    </tr>
    <tr>
    </tr>
  </tbody>
</table>

<br>

<%= link_to 'New Story', new_story_path %>

